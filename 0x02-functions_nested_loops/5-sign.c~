#include "main.h"
/**
 * print_sign- prints the sign of a number.
 * @n : number to check
 * Return:0 or 1
 */

int print_sign(int n)
{
if (n > 0)
{
return (1);
_putchar('+');
}
elseif (n == 0)
{
return (0);
-putchar(0);
}
else
{
return (-1);
_putchar('-');
}
}
