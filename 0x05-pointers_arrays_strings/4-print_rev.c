#include "main.h"

/**
 * rint_rev- prints a string, in reverse, followed by a new line
 *@s : pointer
 * Return: void
 */

void print_rev(char *s)
{
int i = ;

while (str[i])
i++;
while (i--)
{
_putchar(s[i]);
}
_putchar('\n');
}
